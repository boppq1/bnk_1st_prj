<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.admin.dao.IAdminNewsDao">
	<insert id="makeNews">
		insert into news values (SEQ_NEWS.nextval, #{news_ttl}, #{news_cont}, #{news_wtr_no}, sysdate, 0)
	</insert>

	<select id="getNews"  resultType="com.example.demo.admin.dto.NewsDto">
		select * from news ns join admin_users au on ns.news_wtr_no = au.adm_no
	</select>
	
	<select id="getOneNews"  resultType="com.example.demo.admin.dto.NewsDto">
		select * from news ns join admin_users au on ns.news_wtr_no = au.adm_no where news_no = #{news_no}
	</select>
	
	<update id="updateNews">
		update news set news_ttl = #{news_ttl}, news_cont = #{news_cont} where news_no = #{news_no}
	</update>
	
	<delete id="deleteNews">
		delete from news where news_no = #{news_no}
	</delete>
</mapper>